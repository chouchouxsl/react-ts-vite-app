declare module '*/settings.json' {
    const value: {
        colorWeek: boolean
        navbar: boolean
        menu: boolean
        footer: boolean
        themeColor: string
        menuWidth: number
    }

    export default value
}
